server:
  port: 8081 # 项目启动的端口

spring:
  profiles:
    active: dev # 默认激活 dev 本地开发环境

###################### minio：存储图片 #####################
minio:
  endpoint: http://127.0.0.1:9000
  accessKey: dobby
  secretKey: dobby123456