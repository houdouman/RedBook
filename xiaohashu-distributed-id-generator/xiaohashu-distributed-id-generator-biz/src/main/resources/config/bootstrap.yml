spring:
  application:
    name: xiaohashu-distributed-id-generator # 用户服务
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        namespace: xiaohashu
        enabled: true
        group: DEFAULT_GROUP
        server-addr: 127.0.0.1:8848